// Online C compiler to run C program online
#include <stdio.h>
int add(int a, int b);
int mul(int a, int b);
int div(int a, int b);
int sub(int a, int b);
int main() {
   int sum;
   int product;
   int remainder;
   int value;
  sum=add(10,20);
  product=mul(10,20);
  remainder=div(10,20);
  value=sub(10,20);
   
   printf(" Sum= %d", sum );
   printf("\n Product= %d", product);
    printf("\n remainder= %d", remainder);
   printf("\n Substraction= %d", value);
    return 0;
}
int add(int a, int b)
{
    return a+b;
}
int mul(int a, int b)
{
    return a*b;
}
int div(int a, int b)
{
    return a/b;
}
int sub(int a, int b)
{
    return a-b;
}